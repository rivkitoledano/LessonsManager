<Window x:Class="LessonsManager.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="                                                           "
        xmlns:local="clr-namespace:LessonsManager"
        xmlns:controls="clr-namespace:LessonsManager.Controls"
        Title="מסך מנהל" Height="750" Width="1200"
        WindowState="Maximized" WindowStyle="None">
    <Grid Background="#F8F9FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50">
            <StackPanel Orientation="Horizontal" Margin="30,0" VerticalAlignment="Center">
                <Button Content="← חזור" x:Name="BackButton" 
                        Background="#E74C3C" Foreground="White" 
                        Padding="20,12" Margin="0,0,30,0"
                        BorderThickness="0" Cursor="Hand" FontSize="14"
                        Click="BackButton_Click"/>
                <TextBlock Text="מסך מנהל - ניהול שיעורים" FontSize="28" FontWeight="SemiBold" 
                           Foreground="White" VerticalAlignment="Center"/>

                <!-- כפתור יציאה ממצב קיוסק - למנהל בלבד -->
                <Button x:Name="ExitKioskButton"
                        Content="יציאה ממצב קיוסק"
                        FontSize="14"
                        Margin="30,0,0,0"
                        Padding="20,8"
                        Background="#E67E22"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="ExitKioskButton_Click"/>
            </StackPanel>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1.8*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Panel - Upload Lesson -->
            <Border Grid.Column="0" Margin="30,30,15,30" Background="White" 
                    CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="5" 
                                      BlurRadius="15" Opacity="0.1"/>
                </Border.Effect>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="30">
                    <StackPanel>
                        <TextBlock Text="העלאת שיעור חדש" FontSize="22" FontWeight="SemiBold" 
                                   Foreground="#2C3E50" Margin="0,0,0,25"/>
                        
                        <TextBlock Text="נושא:" FontSize="14" FontWeight="Medium" 
                                   Foreground="#34495E" Margin="0,5"/>
                        <ComboBox x:Name="SubjectComboBox" FontSize="14" Height="36" 
                                  Background="White" Margin="0,5,0,20"
                                  IsEditable="True" BorderThickness="1" BorderBrush="#DDD"/>
                        
                        <TextBlock Text="תת-נושא:" FontSize="14" FontWeight="Medium" 
                                   Foreground="#34495E" Margin="0,5"/>
                        <ComboBox x:Name="SubSubjectComboBox" FontSize="14" Height="36" 
                                  Background="White" Margin="0,5,0,20"
                                  IsEditable="True" BorderThickness="1" BorderBrush="#DDD"/>
                        
                        <TextBlock Text="שם שיעור:" FontSize="14" FontWeight="Medium" 
                                   Foreground="#34495E" Margin="0,5"/>
                        <TextBox x:Name="LessonTitleTextBox" FontSize="14" Height="36" 
                                 Background="White" Margin="0,5,0,20"
                                 BorderThickness="1" BorderBrush="#DDD"/>
                        
                        <TextBlock Text="שנה:" FontSize="14" FontWeight="Medium" 
                                   Foreground="#34495E" Margin="0,5"/>
                        <ComboBox x:Name="YearComboBox" FontSize="14" Height="36" 
                                  Background="White" Margin="0,5,0,20"
                                  BorderThickness="1" BorderBrush="#DDD">
                            <ComboBoxItem Content="2024"/>
                            <ComboBoxItem Content="2023"/>
                            <ComboBoxItem Content="2022"/>
                            <ComboBoxItem Content="2021"/>
                        </ComboBox>
                        
                        <TextBlock Text="בחר קובץ MP3:" FontSize="14" FontWeight="Medium" 
                                   Foreground="#34495E" Margin="0,5"/>
                        <Grid Margin="0,5,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="FilePathTextBox" Grid.Column="0" FontSize="14" Height="36" 
                                     Background="White" IsReadOnly="True"
                                     BorderThickness="1" BorderBrush="#DDD"/>
                            <Button x:Name="BrowseButton" Grid.Column="1" Content="עיין" 
                                    FontSize="13" Padding="15,8" Margin="10,0,0,0"
                                    Background="#3498DB" Foreground="White"
                                    BorderThickness="0" Cursor="Hand"
                                    Click="BrowseButton_Click"/>
                        </Grid>
                        
                        <TextBlock Text="בחר קובץ PDF (אופציונלי):" FontSize="14" FontWeight="Medium" 
                                   Foreground="#34495E" Margin="0,5"/>
                        <Grid Margin="0,5,0,25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="PdfPathTextBox" Grid.Column="0" FontSize="14" Height="36" 
                                     Background="White" IsReadOnly="True"
                                     BorderThickness="1" BorderBrush="#DDD"/>
                            <Button x:Name="BrowsePdfButton" Grid.Column="1" Content="עיין PDF" 
                                    FontSize="13" Padding="15,8" Margin="10,0,0,0"
                                    Background="#9B59B6" Foreground="White"
                                    BorderThickness="0" Cursor="Hand"
                                    Click="BrowsePdfButton_Click"/>
                        </Grid>
                        
                        <Button x:Name="UploadButton" Content="העלאת שיעור" 
                                FontSize="15" Padding="40,18" 
                                Background="#27AE60" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Click="UploadButton_Click"/>
                        
                        <TextBlock x:Name="UploadStatus" FontSize="13" 
                                   Foreground="#E67E22" Margin="0,15,0,0"
                                   TextWrapping="Wrap" TextAlignment="Center"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            
            <!-- Right Panel - Existing Lessons -->
            <Border Grid.Column="1" Margin="15,30,30,30" Background="White" 
                    CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="5" 
                                      BlurRadius="15" Opacity="0.1"/>
                </Border.Effect>
                <Grid Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="שיעורים קיימים" FontSize="22" FontWeight="SemiBold" 
                               Foreground="#2C3E50" Margin="0,0,0,20"/>
                    
                    <!-- Navigation Bar -->
                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Button Grid.Column="0" x:Name="NavBackButton" Content="← חזור" 
                                FontSize="12" Padding="10,5" 
                                Background="#95A5A6" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Click="NavBackButton_Click"/>
                        
                        <TextBlock Grid.Column="1" x:Name="PathTextBlock" Text="שורש" 
                                   FontSize="14" Foreground="#7F8C8D" 
                                   VerticalAlignment="Center" Margin="10,0"/>
                        
                        <Button Grid.Column="2" x:Name="RootButton" Content="🏠 שורש" 
                                FontSize="12" Padding="10,5" 
                                Background="#3498DB" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Click="RootButton_Click"/>
                    </Grid>
                    
                    <controls:SimpleTreeView x:Name="LessonsTreeView" Grid.Row="2" Height="400" 
                                             Padding="15" AllowDrop="True"
                                             Drop="LessonsTreeView_Drop">
                        <controls:SimpleTreeView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,10,0">
                                        <TextBlock FontSize="16" VerticalAlignment="Center" Margin="0,0,5,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="🎵"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Name}" Value="..">
                                                            <Setter Property="Text" Value="⬆️"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsFolder}" Value="True">
                                                            <Setter Property="Text" Value="📁"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="▶" FontSize="12" VerticalAlignment="Center" 
                                                  Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </StackPanel>
                                    
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="14" VerticalAlignment="Center"/>
                                    
                                    <TextBlock Grid.Column="2" Text="{Binding YearText}" FontSize="12" 
                                               Foreground="#7F8C8D" VerticalAlignment="Center" HorizontalAlignment="Right"
                                               Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </Grid>
                            </DataTemplate>
                        </controls:SimpleTreeView.ItemTemplate>
                    </controls:SimpleTreeView>
                    
                    <TextBlock Grid.Row="1" Text="גרור תיקיות לכאן כדי להעביר לאונקי" 
                               FontSize="12" Foreground="#95A5A6" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Visibility="Collapsed" x:Name="DropHintText"/>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" 
                                HorizontalAlignment="Center" Margin="0,20,0,0">
                        <Button x:Name="DeleteButton" Content="מחק שיעור" 
                                FontSize="13" Padding="25,12" 
                                Background="#E74C3C" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Click="DeleteButton_Click"/>
                        <Button x:Name="RefreshButton" Content="רענן רשימה" 
                                FontSize="13" Padding="25,12" Margin="15,0,0,0"
                                Background="#3498DB" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Click="RefreshButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Footer -->
        <Border Grid.Row="2" Background="#ECF0F1">
            <TextBlock Text="כל השיעורים נשמרים באופן מקומי במחשב • ניתן למחוק רק שיעורים ספציפיים" 
                       FontSize="12" Foreground="#7F8C8D" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>