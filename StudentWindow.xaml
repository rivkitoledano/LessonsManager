<Window x:Class="LessonsManager.StudentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LessonsManager"
        xmlns:controls="clr-namespace:LessonsManager.Controls"
        mc:Ignorable="d"
        Title="מסך תלמיד" Height="700" Width="1000"
        WindowState="Maximized" WindowStyle="None">
    <Grid Background="#F8F9FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50">
            <StackPanel Orientation="Horizontal" Margin="30,0" VerticalAlignment="Center">
                <Button Content="← חזור" x:Name="BackButton" 
                        Background="#E74C3C" Foreground="White" 
                        Padding="20,12" Margin="0,0,30,0"
                        BorderThickness="0" Cursor="Hand" FontSize="14"
                        Click="BackButton_Click"/>
                <TextBlock Text="מסך תלמיד" FontSize="28" FontWeight="SemiBold" 
                           Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Panel - Device Connection -->
            <Border Grid.Column="0" Margin="30,30,15,30" Background="White" 
                    CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="10" 
                                      BlurRadius="15" Opacity="0.1"/>
                </Border.Effect>
                <StackPanel Margin="30">
                    <TextBlock Text="חיבור התקן" FontSize="22" FontWeight="SemiBold" 
                               Foreground="#2C3E50" Margin="0,0,0,20"/>
                    
                    <TextBlock Text="חבר התקן כדי להתחיל להוריד שיעורים" FontSize="14" 
                               Foreground="#7F8C8D" Margin="0,0,0,30" TextWrapping="Wrap"/>
                    
                    <Button x:Name="ConnectDeviceButton" Content="חבר התקן" 
                            FontSize="16" Padding="40,18" 
                            Background="#27AE60" Foreground="White"
                            BorderThickness="0" Cursor="Hand"
                            Click="ConnectDeviceButton_Click"/>
                    
                    <TextBlock x:Name="DeviceStatus" FontSize="14" 
                               Foreground="#7F8C8D" HorizontalAlignment="Center" 
                               Margin="0,20,0,0" Text="לא מחובר להתקן"/>
                </StackPanel>
            </Border>
            
            <!-- Right Panel - Lessons -->
            <Border Grid.Column="1" Margin="15,30,30,30" Background="White" 
                    CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="10" 
                                      BlurRadius="15" Opacity="0.1"/>
                </Border.Effect>
                <StackPanel Margin="30">
                    <TextBlock Grid.Row="0" Text="שיעורים זמינים" FontSize="22" FontWeight="SemiBold" 
                               Foreground="#2C3E50" Margin="0,0,0,20"/>
                    
                    <!-- Navigation Bar -->
                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Button Grid.Column="0" x:Name="NavBackButton" Content="← חזור" 
                                FontSize="12" Padding="10,5" 
                                Background="#95A5A6" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Click="NavBackButton_Click"/>
                        
                        <TextBlock Grid.Column="1" x:Name="PathTextBlock" Text="שורש" 
                                   FontSize="14" Foreground="#7F8C8D" 
                                   VerticalAlignment="Center" Margin="10,0"/>
                        
                        <Button Grid.Column="2" x:Name="RootButton" Content="🏠 שורש" 
                                FontSize="12" Padding="10,5" 
                                Background="#3498DB" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Click="RootButton_Click"/>
                    </Grid>
                    
                    <controls:SimpleTreeView x:Name="LessonsTreeView" Grid.Row="2" Height="300" 
                                 Background="#F8F9FA" FontSize="13"
                                 BorderThickness="1" BorderBrush="#E1E8ED"
                                 Padding="10">
                        <controls:SimpleTreeView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,10,0">
                                        <TextBlock FontSize="16" VerticalAlignment="Center" Margin="0,0,5,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="🎵"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Name}" Value="..">
                                                            <Setter Property="Text" Value="⬆️"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsFolder}" Value="True">
                                                            <Setter Property="Text" Value="📁"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="▶" FontSize="12" VerticalAlignment="Center" 
                                                  Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </StackPanel>
                                    
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="14" VerticalAlignment="Center"/>
                                    
                                    <TextBlock Grid.Column="2" Text="{Binding YearText}" FontSize="12" 
                                               Foreground="#7F8C8D" VerticalAlignment="Center" HorizontalAlignment="Right"
                                               Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </Grid>
                            </DataTemplate>
                        </controls:SimpleTreeView.ItemTemplate>
                    </controls:SimpleTreeView>
                        
                        <Button x:Name="DownloadButton" Content="הורד שיעור נבחר" 
                                FontSize="15" Padding="35,15" Margin="0,20,0,0"
                                Background="#3498DB" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Click="DownloadButton_Click"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Footer -->
        <Border Grid.Row="2" Background="#ECF0F1">
            <TextBlock Text="התקן חייב להיות מחובר כדי להוריד שיעורים • כל השיעורים נשמרים באופן מקומי" 
                       FontSize="12" Foreground="#7F8C8D" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
